Just experimenting with constructors to see how object creation actually works.

Tried creating objects:

with values → works as expected
without values → saw how Java handles defaults

 Learned:

Constructors run automatically when object is created
If I don’t pass values, variables get default values (null, 0, etc.)
Using a constructor helps set things properly from the start